"""Integration tests for deepgait v2.0.

End-to-end tests spanning multiple layers (e.g. camera→HDF5→gait_algorithms).
Mark with @pytest.mark.integration. These may use synthetic fixtures and do
NOT require physical hardware (use @pytest.mark.hardware for that).
"""
